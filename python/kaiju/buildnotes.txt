if eigen isn't system installed get from here:

http://bitbucket.org/eigen/eigen/get/3.3.7.tar.bz2

unzip and follow install instructions.

rename the source directory to eigen3 and put it in the same directory
as kaiju code so headers can be found.

the compiler may need to be changed from clang++ to g++ if on a linux system
in makefile...


-------pybind------

https://github.com/pybind/pybind11/archive/v2.2.4.tar.gz

or ?

conda install -c conda-forge pybind11

clang++ -O3 -Wall -shared -std=c++11 -undefined dynamic_lookup `python2 -m pybind11 --includes` example.cpp -o example.so

https://pybind11.readthedocs.io/en/master/compiling.html#building-manually



--------------
do constructors like this?

struct Pet {
    enum Kind {
        Dog = 0,
        Cat
    };

    Pet(const std::string &name, Kind type) : name(name), type(type) { }

    std::string name;
    Kind type;
};



lsst?

https://developer.lsst.io/v/DM-9089/coding/python_wrappers_for_cpp_with_pybind11.html